define([
	'coreJS/adapt',
	'./adapt-pageExtension'
], function(Adapt) {

	var articleSlider = _.extend({

		initialize: function() {
			//initialising the block slider extension
		}

	}, Backbone.Events);

	articleSlider.initialize();

});